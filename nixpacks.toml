[phases.setup]
  nixPkgs = ["python310", "python3-pip", "freetype", "libpng"]

[phases.install]
  cmds = ["python3 -m pip install -r requirements.txt"]

[phases.build]
  cmds = []

[start]
  cmd = "python main.py"
